import { Router } from 'express';
import { deleteSession } from '../controllers/controllers.js';

const router = Router();

router.get("/:id")
router.post("/")
router.put("/:id")
router.delete("/:id");

export default router;